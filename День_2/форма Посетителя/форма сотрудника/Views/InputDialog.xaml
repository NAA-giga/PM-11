<Window x:Class="форма_сотрудника.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:форма_сотрудника.Views"
        mc:Ignorable="d"
       Title="Ввод данных" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="QuestionText" Grid.Row="0" Text="Введите значение:" 
                   FontSize="14" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <TextBox x:Name="AnswerBox" Grid.Row="1" Height="60" 
                 TextWrapping="Wrap" AcceptsReturn="True" 
                 Margin="0,0,0,20" FontSize="12"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OkButton_Click" Width="80" Height="30" Margin="5"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="80" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
