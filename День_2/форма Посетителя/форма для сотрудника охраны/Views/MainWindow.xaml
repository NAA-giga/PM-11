<Window x:Class="форма_для_сотрудника_охраны.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:форма_для_сотрудника_охраны"
        mc:Ignorable="d"
      Title="Терминал охраны - ХранительПРО" 
        Height="550" Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ХранительПРО - Терминал охраны" 
                   FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" 
                   Foreground="#2C3E50" Margin="0,0,0,20"/>

        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" 
                Padding="15" Margin="0,0,0,20" Background="#F8F9FA">
            <StackPanel>
                <TextBlock Text="{Binding WelcomeMessage}" FontSize="16" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <Separator Margin="0,5" Height="1" Background="Gray"/>
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Сотрудник:" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Grid.Column="1" Text="{Binding FullName}" FontSize="14"/>
                    <TextBlock Grid.Row="1" Text="Подразделение:" FontWeight="Bold" FontSize="14" Margin="0,5,0,0"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Department}" FontSize="14" Margin="0,5,0,0"/>
                </Grid>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,20">
            <Button Content="📋 Список одобренных заявок" 
                    Command="{Binding ViewRequestsCommand}" 
                    Width="280" Height="50" Margin="10" FontSize="14"
                    Background="#3498DB" Foreground="White" Cursor="Hand"/>

            <Button Content="🔍 Поиск заявки" 
                    Command="{Binding SearchRequestsCommand}" 
                    Width="280" Height="50" Margin="10" FontSize="14"
                    Background="#2ECC71" Foreground="White" Cursor="Hand"/>

            <Separator Margin="0,15,0,15" Height="1" Background="LightGray"/>

            <Button Content="🚪 Выход" 
                    Command="{Binding LogoutCommand}" 
                    Width="280" Height="50" Margin="10" FontSize="14"
                    Background="#E74C3C" Foreground="White" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
