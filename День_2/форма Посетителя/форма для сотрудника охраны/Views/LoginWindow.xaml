<Window x:Class="форма_для_сотрудника_охраны.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:форма_для_сотрудника_охраны.Views"
        mc:Ignorable="d"
       Title="Авторизация охраны - ХранительПРО" 
        Height="380" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Вход в систему ХранительПРО" 
                   FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Separator Grid.Row="1" Grid.ColumnSpan="2" Margin="0,10" Height="2" Background="Gray"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Код сотрудника:" VerticalAlignment="Center" FontSize="14"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding EmployeeCode, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="5" Height="30" FontSize="14"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Пароль:" VerticalAlignment="Center" FontSize="14"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Grid.Column="1" Margin="5" Height="30" FontSize="14"/>

        <Button Grid.Row="4" Grid.ColumnSpan="2" Content="Войти" Command="{Binding LoginCommand}" 
                Width="120" Height="40" Margin="10" FontSize="14" HorizontalAlignment="Center"
                Background="#3498DB" Foreground="White" Cursor="Hand"/>

        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Text="{Binding StatusMessage}" 
                   Foreground="Red" HorizontalAlignment="Center" Margin="10" FontSize="12"/>

        <ProgressBar Grid.Row="6" Grid.ColumnSpan="2" IsIndeterminate="{Binding IsLoading}" 
                     Height="3" Margin="10" VerticalAlignment="Bottom"/>
    </Grid>
</Window>
